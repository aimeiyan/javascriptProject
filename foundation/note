1、函数：http://javascript.info/tutorial/functions-declarations-and-expressions
   array:

2、运行结果，为什么？

setTimeout(function factorial(n) {
    return n == 1 ? n : n*factorial(n-1)
  }, 100)

3、A function is an action. So it’s name should be a verb, like get, read, calculateSum, etc.

4、getElementsByClassName() 兼容IE9+；
兼容老浏览器的方法：


5、下面代码竟然能输出“alert(username.value)”：
<form>
    <input type="text" name="username" value="">
    <input type="button" value="Echo Username" onclick="alert(username.value)">
</form>


6、http://www.wrox.com/WileyCDA/WroxTitle/Professional-JavaScript-for-Web-Developers-3rd-Edition.productCd-1118026691,descCd-DOWNLOAD.html
下载js高级程序设计code页面

7、
    addHandler: function(element, type, handler){
        if (element.addEventListener){
            element.addEventListener(type, handler, false);
        } else if (element.attachEvent){
            element.attachEvent("on" + type, handler);
        } else {
            element["on" + type] = handler;
        }
    },

    问题，为什么？：
        element["on" + type] = handler;

